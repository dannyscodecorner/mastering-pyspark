"""Editable Incan content migrated from the captured core-abstractions-reference.html."""

from surface import Attribute, Chapter, Frame, NodeKind, Slide, Surface, TeachingStep, attr, code, code_block, emphasis, group, heading, image, line_break, node, paragraph, prose, rich_heading, span, strong, text
from components import step_rail, step_cues

pub def slide_section_7() -> Slide:
    return Slide(
        slug="section-7",
        title="Working with PySpark",
        frame=Frame.Composed,
        styles="slide dark",
        content=[
            group([
            text("07 / WORKING WITH PYSPARK"),
        ], style="eyebrow"),
            group([
            span("07", style="chapter-number"),
            group([
                rich_heading([
                    text("Working with"),
                    line_break(),
                    text("PySpark"),
                ], level=1),
                prose("Data preparation, deployment and AWS Glue"),
            ]),
        ], style="divider"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("51"),
        ]),
        ],
        notes="",
    )

pub def slide_data_work() -> Slide:
    return Slide(
        slug="data-work",
        title="Data Preparation with Spark SQL",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("07 / WORKING WITH PYSPARK"),
        ], style="eyebrow"),
            heading("Data Preparation with Spark SQL", level=1),
            node(NodeKind.OrderedList, children=[
            node(NodeKind.ListItem, children=[
                span("01"),
                text("Read, define schemas and explore the input"),
            ]),
            node(NodeKind.ListItem, children=[
                span("02"),
                text("Clean types, timestamps, nulls and nested data"),
            ]),
            node(NodeKind.ListItem, children=[
                span("03"),
                text("Join without losing track of correctness"),
            ]),
            node(NodeKind.ListItem, children=[
                span("04"),
                text("Aggregate and use window functions"),
            ]),
            node(NodeKind.ListItem, children=[
                span("05"),
                text("Write, control file layout and validate the result"),
            ]),
        ], style="topic-list"),
            span("CONTENT SKELETON", style="draft-label"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("52"),
        ]),
        ],
        notes="Movable practical module: explanation, instructor demonstration or participant exercise. Preserve strings/regex, profiling and non-ML preparation material; no fixed lab timetable.",
    )

pub def slide_spark_connect() -> Slide:
    return Slide(
        slug="spark-connect",
        title="Spark Connect",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("07 / WORKING WITH PYSPARK"),
        ], style="eyebrow"),
            heading("Spark Connect", level=1),
            prose("Classic PySpark and the client–server architecture", style="subtitle"),
            node(NodeKind.OrderedList, children=[
            node(NodeKind.ListItem, children=[
                span("01"),
                text("The classic PySpark process model"),
            ]),
            node(NodeKind.ListItem, children=[
                span("02"),
                text("The Spark Connect client/server boundary"),
            ]),
            node(NodeKind.ListItem, children=[
                span("03"),
                text("What crosses the connection"),
            ]),
            node(NodeKind.ListItem, children=[
                span("04"),
                text("API support, limitations and version compatibility"),
            ]),
        ], style="topic-list"),
            span("CONTENT SKELETON", style="draft-label"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("53"),
        ]),
        ],
        notes="",
    )

pub def slide_production() -> Slide:
    return Slide(
        slug="production",
        title="Running Spark in Production",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("07 / WORKING WITH PYSPARK"),
        ], style="eyebrow"),
            heading("Running Spark in Production", level=1),
            node(NodeKind.OrderedList, children=[
            node(NodeKind.ListItem, children=[
                span("01"),
                text("Launching an application and placing the driver"),
            ]),
            node(NodeKind.ListItem, children=[
                span("02"),
                text("Packaging Python and other dependencies"),
            ]),
            node(NodeKind.ListItem, children=[
                span("03"),
                text("Remote execution interfaces and configuration"),
            ]),
            node(NodeKind.ListItem, children=[
                span("04"),
                text("Monitoring, diagnosis and scaling decisions"),
            ]),
            node(NodeKind.ListItem, children=[
                span("05"),
                text("Reruns, reliable output and deployment environments"),
            ]),
            node(NodeKind.ListItem, children=[
                span("06"),
                text("Mapping those decisions to Glue"),
            ]),
        ], style="topic-list"),
            span("CONTENT SKELETON", style="draft-label"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("54"),
        ]),
        ],
        notes="",
    )

pub def slide_glue() -> Slide:
    return Slide(
        slug="glue",
        title="Spark in AWS Glue",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("07 / WORKING WITH PYSPARK"),
        ], style="eyebrow"),
            heading("Spark in AWS Glue", level=1),
            node(NodeKind.OrderedList, children=[
            node(NodeKind.ListItem, children=[
                span("01"),
                text("Glue jobs, workers and the driver/executor model"),
            ]),
            node(NodeKind.ListItem, children=[
                span("02"),
                text("Glue runtime versions versus local Spark"),
            ]),
            node(NodeKind.ListItem, children=[
                span("03"),
                text("GlueContext, DynamicFrames and DataFrames"),
            ]),
            node(NodeKind.ListItem, children=[
                span("04"),
                text("Catalogs, connectors and data access"),
            ]),
            node(NodeKind.ListItem, children=[
                span("05"),
                text("AQE versus resource scaling; progress and monitoring"),
            ]),
            node(NodeKind.ListItem, children=[
                span("06"),
                text("Streaming, checkpoints, bookmarks and recovery"),
            ]),
        ], style="topic-list"),
            span("CONTENT SKELETON", style="draft-label"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("55"),
        ]),
        ],
        notes="Keep the Glue mapping at the end of this section. Runtime-specific claims remain to be verified against the client environment.",
    )

pub def slide_closing() -> Slide:
    return Slide(
        slug="closing",
        title="Recap and Questions",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("07 / WORKING WITH PYSPARK"),
        ], style="eyebrow"),
            heading("Recap and Questions", level=1),
            group([
            paragraph([
                span("01"),
                text("Where does this code execute?"),
            ]),
            paragraph([
                span("02"),
                text("What work causes data to move?"),
            ]),
            paragraph([
                span("03"),
                text("What evidence explains the behaviour?"),
            ]),
        ], style="closing-prompts"),
            prose("Discussion · recap · next steps", style="takeaway"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("56"),
        ]),
        ],
        notes="Sample recap layout. Resources and client-specific follow-up links to be added.",
    )

pub def core_reference_07_chapter() -> Chapter:
    return Chapter(number="7", title="07 / WORKING WITH PYSPARK", first_page=1, slides=[
        slide_section_7(),
        slide_data_work(),
        slide_spark_connect(),
        slide_production(),
        slide_glue(),
        slide_closing(),
    ])
