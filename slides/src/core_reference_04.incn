"""Editable Incan content migrated from the captured core-abstractions-reference.html."""

from surface import Attribute, Chapter, Frame, NodeKind, Slide, Surface, TeachingStep, attr, code, code_block, emphasis, group, heading, image, line_break, node, paragraph, prose, rich_heading, span, strong, text
from components import step_rail, step_cues

pub def slide_section_4() -> Slide:
    return Slide(
        slug="section-4",
        title="Lazy Pandas …",
        frame=Frame.Composed,
        styles="slide dark",
        content=[
            group([
            text("04 / LAZY PANDAS"),
        ], style="eyebrow"),
            group([
            span("04", style="chapter-number"),
            group([
                heading("Lazy Pandas …", level=1),
                prose("Pandas? … Lazy? …"),
            ]),
        ], style="divider"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("42"),
        ]),
        ],
        notes="",
    )

pub def slide_lazy_pandas() -> Slide:
    return Slide(
        slug="lazy-pandas",
        title="Spark is Lazy",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("04 / LAZY PANDAS"),
        ], style="eyebrow"),
            heading("Spark is Lazy", level=1),
            prose("Pandas vs. Spark DataFrames", style="subtitle"),
            node(NodeKind.OrderedList, children=[
            node(NodeKind.ListItem, children=[
                span("01"),
                text("Pandas and Spark operations side by side"),
            ]),
            node(NodeKind.ListItem, children=[
                span("02"),
                text("Eager versus lazy computation"),
            ]),
            node(NodeKind.ListItem, children=[
                span("03"),
                text("Transformations describe work; actions request results"),
            ]),
            node(NodeKind.ListItem, children=[
                span("04"),
                text("Immutable branches and repeated requests"),
            ]),
            node(NodeKind.ListItem, children=[
                span("05"),
                text("When errors appear—and when work is repeated"),
            ]),
        ], style="topic-list"),
            span("CONTENT SKELETON", style="draft-label"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("43"),
        ]),
        ],
        notes="Workshop slides 22–28. Preserve the Lazy Pandas comparison. Detailed planning comes next, after these semantics are established.",
    )

pub def slide_dataframe_example() -> Slide:
    return Slide(
        slug="dataframe-example",
        title="Transformations and Actions",
        frame=Frame.Composed,
        styles="slide ",
        content=[
            group([
            text("04 / LAZY PANDAS"),
        ], style="eyebrow"),
            heading("Transformations and Actions", level=1),
            node(NodeKind.Preformatted, children=[
            node(NodeKind.Code, content="""result = (events
    .filter(col("amount") > 0)
    .groupBy("category")
    .count()
)

result.show()  # action""", style="language-python"),
        ], attributes=[attr("aria-label", "PySpark transformation example")]),
            paragraph([
            text("Here, "),
            code("groupBy().count()"),
            text(" builds an aggregate. "),
            code("show()"),
            text(" is the action."),
        ], style="takeaway"),
            node(NodeKind.Footer, children=[
            span("Danny’s Code Corner"),
            span("44"),
        ]),
        ],
        notes="Sample code layout, not a runnable notebook. Assumes events is a DataFrame and col is imported from pyspark.sql.functions. Explain that GroupedData.count() returns a DataFrame, unlike DataFrame.count().",
    )

pub def core_reference_04_chapter() -> Chapter:
    return Chapter(number="4", title="04 / LAZY PANDAS", first_page=1, slides=[
        slide_section_4(),
        slide_lazy_pandas(),
        slide_dataframe_example(),
    ])
